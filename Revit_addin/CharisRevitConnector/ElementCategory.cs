namespace CharisRevitConnector;

/// <summary>The element families the connector syncs, one Firestore collection each.</summary>
internal enum ElementCategory
{
    Floor,
    Wall,
    Beam,
    Column,
}
